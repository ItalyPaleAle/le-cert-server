// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// Com35Config holds configuration for the "com35" DNS provider (35.com/三五互联)
// See https://www.35.cn/
type Com35Config struct {
	Password           string // COM35_PASSWORD: API password
	Username           string // COM35_USERNAME: Username
	HTTPTimeout        string // COM35_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // COM35_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 10)
	PropagationTimeout string // COM35_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 120)
	TTL                string // COM35_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 60)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *Com35Config) envVars() map[string]string {
	m := make(map[string]string, 6)
	if c.Password != "" {
		m["COM35_PASSWORD"] = c.Password
	}
	if c.Username != "" {
		m["COM35_USERNAME"] = c.Username
	}
	if c.HTTPTimeout != "" {
		m["COM35_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["COM35_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["COM35_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["COM35_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *Com35Config) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"com35\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "password", "COM35_PASSWORD":
			c.Password = val
		case "username", "COM35_USERNAME":
			c.Username = val
		case "httpTimeout", "COM35_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "COM35_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "COM35_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "COM35_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"com35\"", key)
		}
	}
	return nil
}
