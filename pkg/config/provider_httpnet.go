// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"
	"strconv"
	"time"

	"github.com/go-acme/lego/v4/challenge"
	prov "github.com/go-acme/lego/v4/providers/dns/httpnet"
	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// HttpnetConfig holds configuration for the "httpnet" DNS provider (http.net)
// See https://www.http.net/
type HttpnetConfig struct {
	APIKey             string // HTTPNET_API_KEY: API key
	PollingInterval    string // HTTPNET_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // HTTPNET_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 120)
	TTL                string // HTTPNET_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 120)
	ZoneName           string // HTTPNET_ZONE_NAME: Zone name in ACE format
}

// newProvider builds the lego DNS challenge provider using strong types
// Credentials are passed directly to lego and never written to the process environment
func (c *HttpnetConfig) newProvider() (challenge.Provider, error) {
	cfg := prov.NewDefaultConfig()
	if c.APIKey != "" {
		cfg.APIKey = c.APIKey
	}
	if c.PollingInterval != "" {
		v, err := strconv.Atoi(c.PollingInterval)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"pollingInterval\": %w", err)
		}
		cfg.PollingInterval = time.Duration(v) * time.Second
	}
	if c.PropagationTimeout != "" {
		v, err := strconv.Atoi(c.PropagationTimeout)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"propagationTimeout\": %w", err)
		}
		cfg.PropagationTimeout = time.Duration(v) * time.Second
	}
	if c.TTL != "" {
		v, err := strconv.Atoi(c.TTL)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"ttl\": %w", err)
		}
		cfg.TTL = v
	}
	if c.ZoneName != "" {
		cfg.ZoneName = c.ZoneName
	}
	return prov.NewDNSProviderConfig(cfg)
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *HttpnetConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"httpnet\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "apiKey", "HTTPNET_API_KEY":
			c.APIKey = val
		case "pollingInterval", "HTTPNET_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "HTTPNET_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "HTTPNET_TTL":
			c.TTL = val
		case "zoneName", "HTTPNET_ZONE_NAME":
			c.ZoneName = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"httpnet\"", key)
		}
	}
	return nil
}
