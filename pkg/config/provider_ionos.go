// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// IonosConfig holds configuration for the "ionos" DNS provider (Ionos)
// See https://ionos.com
type IonosConfig struct {
	APIKey             string // IONOS_API_KEY: API key `<prefix>.<secret>` https://developer.hosting.ionos.com/docs/getstarted
	HTTPTimeout        string // IONOS_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // IONOS_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // IONOS_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 900)
	TTL                string // IONOS_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 300)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *IonosConfig) envVars() map[string]string {
	m := make(map[string]string, 5)
	if c.APIKey != "" {
		m["IONOS_API_KEY"] = c.APIKey
	}
	if c.HTTPTimeout != "" {
		m["IONOS_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["IONOS_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["IONOS_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["IONOS_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *IonosConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"ionos\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "apiKey", "IONOS_API_KEY":
			c.APIKey = val
		case "httpTimeout", "IONOS_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "IONOS_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "IONOS_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "IONOS_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"ionos\"", key)
		}
	}
	return nil
}
