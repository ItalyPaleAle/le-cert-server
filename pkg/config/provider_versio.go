// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// VersioConfig holds configuration for the "versio" DNS provider (Versio.[nl|eu|uk])
// See https://www.versio.nl/domeinnamen
type VersioConfig struct {
	Password           string // VERSIO_PASSWORD: Basic authentication password
	Username           string // VERSIO_USERNAME: Basic authentication username
	Endpoint           string // VERSIO_ENDPOINT: The endpoint URL of the API Server
	HTTPTimeout        string // VERSIO_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // VERSIO_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 5)
	PropagationTimeout string // VERSIO_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	SequenceInterval   string // VERSIO_SEQUENCE_INTERVAL: Time between sequential requests in seconds (Default: 60)
	TTL                string // VERSIO_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 300)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *VersioConfig) envVars() map[string]string {
	m := make(map[string]string, 8)
	if c.Password != "" {
		m["VERSIO_PASSWORD"] = c.Password
	}
	if c.Username != "" {
		m["VERSIO_USERNAME"] = c.Username
	}
	if c.Endpoint != "" {
		m["VERSIO_ENDPOINT"] = c.Endpoint
	}
	if c.HTTPTimeout != "" {
		m["VERSIO_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["VERSIO_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["VERSIO_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.SequenceInterval != "" {
		m["VERSIO_SEQUENCE_INTERVAL"] = c.SequenceInterval
	}
	if c.TTL != "" {
		m["VERSIO_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *VersioConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"versio\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "password", "VERSIO_PASSWORD":
			c.Password = val
		case "username", "VERSIO_USERNAME":
			c.Username = val
		case "endpoint", "VERSIO_ENDPOINT":
			c.Endpoint = val
		case "httpTimeout", "VERSIO_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "VERSIO_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "VERSIO_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "sequenceInterval", "VERSIO_SEQUENCE_INTERVAL":
			c.SequenceInterval = val
		case "ttl", "VERSIO_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"versio\"", key)
		}
	}
	return nil
}
