// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"
	"net/url"
	"strconv"
	"time"

	"github.com/go-acme/lego/v4/challenge"
	prov "github.com/go-acme/lego/v4/providers/dns/httpreq"
	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// HttpreqConfig holds configuration for the "httpreq" DNS provider (HTTP request)
// See /lego/dns/httpreq/
type HttpreqConfig struct {
	Endpoint           string // HTTPREQ_ENDPOINT: The URL of the server
	Mode               string // HTTPREQ_MODE: `RAW`, none
	Password           string // HTTPREQ_PASSWORD: Basic authentication password
	PollingInterval    string // HTTPREQ_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // HTTPREQ_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	Username           string // HTTPREQ_USERNAME: Basic authentication username
}

// newProvider builds the lego DNS challenge provider using strong types
// Credentials are passed directly to lego and never written to the process environment
func (c *HttpreqConfig) newProvider() (challenge.Provider, error) {
	cfg := prov.NewDefaultConfig()
	if c.Endpoint != "" {
		v, err := url.Parse(c.Endpoint)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"endpoint\": %w", err)
		}
		cfg.Endpoint = v
	}
	if c.Mode != "" {
		cfg.Mode = c.Mode
	}
	if c.Password != "" {
		cfg.Password = c.Password
	}
	if c.PollingInterval != "" {
		v, err := strconv.Atoi(c.PollingInterval)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"pollingInterval\": %w", err)
		}
		cfg.PollingInterval = time.Duration(v) * time.Second
	}
	if c.PropagationTimeout != "" {
		v, err := strconv.Atoi(c.PropagationTimeout)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"propagationTimeout\": %w", err)
		}
		cfg.PropagationTimeout = time.Duration(v) * time.Second
	}
	if c.Username != "" {
		cfg.Username = c.Username
	}
	return prov.NewDNSProviderConfig(cfg)
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *HttpreqConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"httpreq\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "endpoint", "HTTPREQ_ENDPOINT":
			c.Endpoint = val
		case "mode", "HTTPREQ_MODE":
			c.Mode = val
		case "password", "HTTPREQ_PASSWORD":
			c.Password = val
		case "pollingInterval", "HTTPREQ_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "HTTPREQ_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "username", "HTTPREQ_USERNAME":
			c.Username = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"httpreq\"", key)
		}
	}
	return nil
}
