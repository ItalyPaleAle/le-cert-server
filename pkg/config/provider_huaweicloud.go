// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"
	"strconv"
	"time"

	"github.com/go-acme/lego/v4/challenge"
	prov "github.com/go-acme/lego/v4/providers/dns/huaweicloud"
	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// HuaweicloudConfig holds configuration for the "huaweicloud" DNS provider (Huawei Cloud)
// See https://huaweicloud.com
type HuaweicloudConfig struct {
	AccessKeyID        string // HUAWEICLOUD_ACCESS_KEY_ID: Access key ID
	Region             string // HUAWEICLOUD_REGION: Region
	SecretAccessKey    string // HUAWEICLOUD_SECRET_ACCESS_KEY: Access Key secret
	HTTPTimeout        string // HUAWEICLOUD_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // HUAWEICLOUD_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // HUAWEICLOUD_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
}

// newProvider builds the lego DNS challenge provider using strong types
// Credentials are passed directly to lego and never written to the process environment
func (c *HuaweicloudConfig) newProvider() (challenge.Provider, error) {
	cfg := prov.NewDefaultConfig()
	if c.AccessKeyID != "" {
		cfg.AccessKeyID = c.AccessKeyID
	}
	if c.Region != "" {
		cfg.Region = c.Region
	}
	if c.SecretAccessKey != "" {
		cfg.SecretAccessKey = c.SecretAccessKey
	}
	if c.HTTPTimeout != "" {
		v, err := strconv.Atoi(c.HTTPTimeout)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"httpTimeout\": %w", err)
		}
		cfg.HTTPTimeout = time.Duration(v) * time.Second
	}
	if c.PollingInterval != "" {
		v, err := strconv.Atoi(c.PollingInterval)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"pollingInterval\": %w", err)
		}
		cfg.PollingInterval = time.Duration(v) * time.Second
	}
	if c.PropagationTimeout != "" {
		v, err := strconv.Atoi(c.PropagationTimeout)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"propagationTimeout\": %w", err)
		}
		cfg.PropagationTimeout = time.Duration(v) * time.Second
	}
	return prov.NewDNSProviderConfig(cfg)
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *HuaweicloudConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"huaweicloud\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "accessKeyID", "HUAWEICLOUD_ACCESS_KEY_ID":
			c.AccessKeyID = val
		case "region", "HUAWEICLOUD_REGION":
			c.Region = val
		case "secretAccessKey", "HUAWEICLOUD_SECRET_ACCESS_KEY":
			c.SecretAccessKey = val
		case "httpTimeout", "HUAWEICLOUD_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "HUAWEICLOUD_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "HUAWEICLOUD_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"huaweicloud\"", key)
		}
	}
	return nil
}
