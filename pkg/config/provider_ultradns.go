// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// UltradnsConfig holds configuration for the "ultradns" DNS provider (Ultradns)
// See https://vercara.com/authoritative-dns
type UltradnsConfig struct {
	Password           string // ULTRADNS_PASSWORD: API Password
	Username           string // ULTRADNS_USERNAME: API Username
	Endpoint           string // ULTRADNS_ENDPOINT: API endpoint URL, defaults to https://api.ultradns.com/
	PollingInterval    string // ULTRADNS_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 4)
	PropagationTimeout string // ULTRADNS_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 120)
	TTL                string // ULTRADNS_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 120)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *UltradnsConfig) envVars() map[string]string {
	m := make(map[string]string, 6)
	if c.Password != "" {
		m["ULTRADNS_PASSWORD"] = c.Password
	}
	if c.Username != "" {
		m["ULTRADNS_USERNAME"] = c.Username
	}
	if c.Endpoint != "" {
		m["ULTRADNS_ENDPOINT"] = c.Endpoint
	}
	if c.PollingInterval != "" {
		m["ULTRADNS_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["ULTRADNS_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["ULTRADNS_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *UltradnsConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"ultradns\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "password", "ULTRADNS_PASSWORD":
			c.Password = val
		case "username", "ULTRADNS_USERNAME":
			c.Username = val
		case "endpoint", "ULTRADNS_ENDPOINT":
			c.Endpoint = val
		case "pollingInterval", "ULTRADNS_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "ULTRADNS_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "ULTRADNS_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"ultradns\"", key)
		}
	}
	return nil
}
