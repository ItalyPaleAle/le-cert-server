// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"
	"strconv"
	"time"

	"github.com/go-acme/lego/v4/challenge"
	prov "github.com/go-acme/lego/v4/providers/dns/vinyldns"
	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// VinyldnsConfig holds configuration for the "vinyldns" DNS provider (VinylDNS)
// See https://www.vinyldns.io
type VinyldnsConfig struct {
	AccessKey          string // VINYLDNS_ACCESS_KEY: The VinylDNS API key
	Host               string // VINYLDNS_HOST: The VinylDNS API URL
	SecretKey          string // VINYLDNS_SECRET_KEY: The VinylDNS API Secret key
	PollingInterval    string // VINYLDNS_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 4)
	PropagationTimeout string // VINYLDNS_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 120)
	QuoteValue         string // VINYLDNS_QUOTE_VALUE: Adds quotes around the TXT record value (Default: false)
	TTL                string // VINYLDNS_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 30)
}

// newProvider builds the lego DNS challenge provider using strong types
// Credentials are passed directly to lego and never written to the process environment
func (c *VinyldnsConfig) newProvider() (challenge.Provider, error) {
	cfg := prov.NewDefaultConfig()
	if c.AccessKey != "" {
		cfg.AccessKey = c.AccessKey
	}
	if c.Host != "" {
		cfg.Host = c.Host
	}
	if c.SecretKey != "" {
		cfg.SecretKey = c.SecretKey
	}
	if c.PollingInterval != "" {
		v, err := strconv.Atoi(c.PollingInterval)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"pollingInterval\": %w", err)
		}
		cfg.PollingInterval = time.Duration(v) * time.Second
	}
	if c.PropagationTimeout != "" {
		v, err := strconv.Atoi(c.PropagationTimeout)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"propagationTimeout\": %w", err)
		}
		cfg.PropagationTimeout = time.Duration(v) * time.Second
	}
	if c.QuoteValue != "" {
		v, err := strconv.ParseBool(c.QuoteValue)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"quoteValue\": %w", err)
		}
		cfg.QuoteValue = v
	}
	if c.TTL != "" {
		v, err := strconv.Atoi(c.TTL)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"ttl\": %w", err)
		}
		cfg.TTL = v
	}
	return prov.NewDNSProviderConfig(cfg)
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *VinyldnsConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"vinyldns\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "accessKey", "VINYLDNS_ACCESS_KEY":
			c.AccessKey = val
		case "host", "VINYLDNS_HOST":
			c.Host = val
		case "secretKey", "VINYLDNS_SECRET_KEY":
			c.SecretKey = val
		case "pollingInterval", "VINYLDNS_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "VINYLDNS_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "quoteValue", "VINYLDNS_QUOTE_VALUE":
			c.QuoteValue = val
		case "ttl", "VINYLDNS_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"vinyldns\"", key)
		}
	}
	return nil
}
