// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// NifcloudConfig holds configuration for the "nifcloud" DNS provider (NIFCloud)
// See https://www.nifcloud.com/
type NifcloudConfig struct {
	AccessKeyID        string // NIFCLOUD_ACCESS_KEY_ID: Access key
	SecretAccessKey    string // NIFCLOUD_SECRET_ACCESS_KEY: Secret access key
	HTTPTimeout        string // NIFCLOUD_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // NIFCLOUD_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // NIFCLOUD_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	TTL                string // NIFCLOUD_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 120)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *NifcloudConfig) envVars() map[string]string {
	m := make(map[string]string, 6)
	if c.AccessKeyID != "" {
		m["NIFCLOUD_ACCESS_KEY_ID"] = c.AccessKeyID
	}
	if c.SecretAccessKey != "" {
		m["NIFCLOUD_SECRET_ACCESS_KEY"] = c.SecretAccessKey
	}
	if c.HTTPTimeout != "" {
		m["NIFCLOUD_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["NIFCLOUD_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["NIFCLOUD_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["NIFCLOUD_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *NifcloudConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"nifcloud\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "accessKeyID", "NIFCLOUD_ACCESS_KEY_ID":
			c.AccessKeyID = val
		case "secretAccessKey", "NIFCLOUD_SECRET_ACCESS_KEY":
			c.SecretAccessKey = val
		case "httpTimeout", "NIFCLOUD_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "NIFCLOUD_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "NIFCLOUD_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "NIFCLOUD_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"nifcloud\"", key)
		}
	}
	return nil
}
