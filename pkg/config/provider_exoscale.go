// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"
	"strconv"
	"time"

	"github.com/go-acme/lego/v4/challenge"
	prov "github.com/go-acme/lego/v4/providers/dns/exoscale"
	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// ExoscaleConfig holds configuration for the "exoscale" DNS provider (Exoscale)
// See https://www.exoscale.com/
type ExoscaleConfig struct {
	APIKey             string // EXOSCALE_API_KEY: API key
	APISecret          string // EXOSCALE_API_SECRET: API secret
	Endpoint           string // EXOSCALE_ENDPOINT: API endpoint URL
	HTTPTimeout        string // EXOSCALE_HTTP_TIMEOUT: API request timeout in seconds (Default: 60)
	PollingInterval    string // EXOSCALE_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // EXOSCALE_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	TTL                string // EXOSCALE_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 120)
}

// newProvider builds the lego DNS challenge provider using strong types
// Credentials are passed directly to lego and never written to the process environment
func (c *ExoscaleConfig) newProvider() (challenge.Provider, error) {
	cfg := prov.NewDefaultConfig()
	if c.APIKey != "" {
		cfg.APIKey = c.APIKey
	}
	if c.APISecret != "" {
		cfg.APISecret = c.APISecret
	}
	if c.Endpoint != "" {
		cfg.Endpoint = c.Endpoint
	}
	if c.HTTPTimeout != "" {
		v, err := strconv.Atoi(c.HTTPTimeout)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"httpTimeout\": %w", err)
		}
		cfg.HTTPTimeout = time.Duration(v) * time.Second
	}
	if c.PollingInterval != "" {
		v, err := strconv.Atoi(c.PollingInterval)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"pollingInterval\": %w", err)
		}
		cfg.PollingInterval = time.Duration(v) * time.Second
	}
	if c.PropagationTimeout != "" {
		v, err := strconv.Atoi(c.PropagationTimeout)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"propagationTimeout\": %w", err)
		}
		cfg.PropagationTimeout = time.Duration(v) * time.Second
	}
	if c.TTL != "" {
		v, err := strconv.ParseInt(c.TTL, 10, 64)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"ttl\": %w", err)
		}
		cfg.TTL = v
	}
	return prov.NewDNSProviderConfig(cfg)
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *ExoscaleConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"exoscale\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "apiKey", "EXOSCALE_API_KEY":
			c.APIKey = val
		case "apiSecret", "EXOSCALE_API_SECRET":
			c.APISecret = val
		case "endpoint", "EXOSCALE_ENDPOINT":
			c.Endpoint = val
		case "httpTimeout", "EXOSCALE_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "EXOSCALE_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "EXOSCALE_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "EXOSCALE_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"exoscale\"", key)
		}
	}
	return nil
}
