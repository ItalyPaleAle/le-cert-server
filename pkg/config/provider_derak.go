// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// DerakConfig holds configuration for the "derak" DNS provider (Derak Cloud)
// See https://derak.cloud/
type DerakConfig struct {
	APIKey             string // DERAK_API_KEY: The API key
	HTTPTimeout        string // DERAK_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // DERAK_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 5)
	PropagationTimeout string // DERAK_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 120)
	TTL                string // DERAK_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 120)
	WebsiteID          string // DERAK_WEBSITE_ID: Force the zone/website ID
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *DerakConfig) envVars() map[string]string {
	m := make(map[string]string, 6)
	if c.APIKey != "" {
		m["DERAK_API_KEY"] = c.APIKey
	}
	if c.HTTPTimeout != "" {
		m["DERAK_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["DERAK_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["DERAK_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["DERAK_TTL"] = c.TTL
	}
	if c.WebsiteID != "" {
		m["DERAK_WEBSITE_ID"] = c.WebsiteID
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *DerakConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"derak\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "apiKey", "DERAK_API_KEY":
			c.APIKey = val
		case "httpTimeout", "DERAK_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "DERAK_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "DERAK_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "DERAK_TTL":
			c.TTL = val
		case "websiteID", "DERAK_WEBSITE_ID":
			c.WebsiteID = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"derak\"", key)
		}
	}
	return nil
}
