// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// EurodnsConfig holds configuration for the "eurodns" DNS provider (EuroDNS)
// See https://www.eurodns.com/
type EurodnsConfig struct {
	APIKey             string // EURODNS_API_KEY: API key
	AppID              string // EURODNS_APP_ID: Application ID
	HTTPTimeout        string // EURODNS_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // EURODNS_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // EURODNS_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	TTL                string // EURODNS_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 600)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *EurodnsConfig) envVars() map[string]string {
	m := make(map[string]string, 6)
	if c.APIKey != "" {
		m["EURODNS_API_KEY"] = c.APIKey
	}
	if c.AppID != "" {
		m["EURODNS_APP_ID"] = c.AppID
	}
	if c.HTTPTimeout != "" {
		m["EURODNS_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["EURODNS_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["EURODNS_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["EURODNS_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *EurodnsConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"eurodns\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "apiKey", "EURODNS_API_KEY":
			c.APIKey = val
		case "appID", "EURODNS_APP_ID":
			c.AppID = val
		case "httpTimeout", "EURODNS_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "EURODNS_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "EURODNS_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "EURODNS_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"eurodns\"", key)
		}
	}
	return nil
}
