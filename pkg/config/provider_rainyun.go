// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// RainyunConfig holds configuration for the "rainyun" DNS provider (Rain Yun/雨云)
// See https://www.rainyun.com
type RainyunConfig struct {
	APIKey             string // RAINYUN_API_KEY: API key
	HTTPTimeout        string // RAINYUN_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // RAINYUN_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // RAINYUN_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 120)
	TTL                string // RAINYUN_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 120)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *RainyunConfig) envVars() map[string]string {
	m := make(map[string]string, 5)
	if c.APIKey != "" {
		m["RAINYUN_API_KEY"] = c.APIKey
	}
	if c.HTTPTimeout != "" {
		m["RAINYUN_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["RAINYUN_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["RAINYUN_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["RAINYUN_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *RainyunConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"rainyun\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "apiKey", "RAINYUN_API_KEY":
			c.APIKey = val
		case "httpTimeout", "RAINYUN_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "RAINYUN_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "RAINYUN_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "RAINYUN_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"rainyun\"", key)
		}
	}
	return nil
}
