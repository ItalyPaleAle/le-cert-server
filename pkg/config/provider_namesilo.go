// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// NamesiloConfig holds configuration for the "namesilo" DNS provider (Namesilo)
// See https://www.namesilo.com/
type NamesiloConfig struct {
	APIKey             string // NAMESILO_API_KEY: Client ID
	PollingInterval    string // NAMESILO_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // NAMESILO_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60), it is better to set larger than 15 minutes
	TTL                string // NAMESILO_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 3600), should be in [3600, 2592000]
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *NamesiloConfig) envVars() map[string]string {
	m := make(map[string]string, 4)
	if c.APIKey != "" {
		m["NAMESILO_API_KEY"] = c.APIKey
	}
	if c.PollingInterval != "" {
		m["NAMESILO_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["NAMESILO_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["NAMESILO_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *NamesiloConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"namesilo\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "apiKey", "NAMESILO_API_KEY":
			c.APIKey = val
		case "pollingInterval", "NAMESILO_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "NAMESILO_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "NAMESILO_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"namesilo\"", key)
		}
	}
	return nil
}
