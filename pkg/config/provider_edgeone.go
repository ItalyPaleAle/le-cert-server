// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"
	"strconv"
	"time"

	"github.com/go-acme/lego/v4/challenge"
	prov "github.com/go-acme/lego/v4/providers/dns/edgeone"
	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// EdgeoneConfig holds configuration for the "edgeone" DNS provider (Tencent EdgeOne)
// See https://edgeone.ai
type EdgeoneConfig struct {
	SecretID           string // EDGEONE_SECRET_ID: Access key ID
	SecretKey          string // EDGEONE_SECRET_KEY: Access Key secret
	HTTPTimeout        string // EDGEONE_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // EDGEONE_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 30)
	PropagationTimeout string // EDGEONE_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 1200)
	Region             string // EDGEONE_REGION: Region
	SessionToken       string // EDGEONE_SESSION_TOKEN: Access Key token
	TTL                string // EDGEONE_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 60)
}

// newProvider builds the lego DNS challenge provider using strong types
// Credentials are passed directly to lego and never written to the process environment
func (c *EdgeoneConfig) newProvider() (challenge.Provider, error) {
	cfg := prov.NewDefaultConfig()
	if c.SecretID != "" {
		cfg.SecretID = c.SecretID
	}
	if c.SecretKey != "" {
		cfg.SecretKey = c.SecretKey
	}
	if c.HTTPTimeout != "" {
		v, err := strconv.Atoi(c.HTTPTimeout)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"httpTimeout\": %w", err)
		}
		cfg.HTTPTimeout = time.Duration(v) * time.Second
	}
	if c.PollingInterval != "" {
		v, err := strconv.Atoi(c.PollingInterval)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"pollingInterval\": %w", err)
		}
		cfg.PollingInterval = time.Duration(v) * time.Second
	}
	if c.PropagationTimeout != "" {
		v, err := strconv.Atoi(c.PropagationTimeout)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"propagationTimeout\": %w", err)
		}
		cfg.PropagationTimeout = time.Duration(v) * time.Second
	}
	if c.Region != "" {
		cfg.Region = c.Region
	}
	if c.SessionToken != "" {
		cfg.SessionToken = c.SessionToken
	}
	if c.TTL != "" {
		v, err := strconv.Atoi(c.TTL)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"ttl\": %w", err)
		}
		cfg.TTL = v
	}
	return prov.NewDNSProviderConfig(cfg)
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *EdgeoneConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"edgeone\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "secretID", "EDGEONE_SECRET_ID":
			c.SecretID = val
		case "secretKey", "EDGEONE_SECRET_KEY":
			c.SecretKey = val
		case "httpTimeout", "EDGEONE_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "EDGEONE_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "EDGEONE_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "region", "EDGEONE_REGION":
			c.Region = val
		case "sessionToken", "EDGEONE_SESSION_TOKEN":
			c.SessionToken = val
		case "ttl", "EDGEONE_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"edgeone\"", key)
		}
	}
	return nil
}
