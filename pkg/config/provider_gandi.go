// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// GandiConfig holds configuration for the "gandi" DNS provider (Gandi)
// See https://www.gandi.net
type GandiConfig struct {
	APIKey             string // GANDI_API_KEY: API key
	HTTPTimeout        string // GANDI_HTTP_TIMEOUT: API request timeout in seconds (Default: 60)
	PollingInterval    string // GANDI_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 60)
	PropagationTimeout string // GANDI_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 2400)
	TTL                string // GANDI_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 300)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *GandiConfig) envVars() map[string]string {
	m := make(map[string]string, 5)
	if c.APIKey != "" {
		m["GANDI_API_KEY"] = c.APIKey
	}
	if c.HTTPTimeout != "" {
		m["GANDI_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["GANDI_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["GANDI_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["GANDI_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *GandiConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"gandi\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "apiKey", "GANDI_API_KEY":
			c.APIKey = val
		case "httpTimeout", "GANDI_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "GANDI_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "GANDI_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "GANDI_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"gandi\"", key)
		}
	}
	return nil
}
