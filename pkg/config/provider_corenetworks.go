// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"
	"strconv"
	"time"

	"github.com/go-acme/lego/v4/challenge"
	prov "github.com/go-acme/lego/v4/providers/dns/corenetworks"
	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// CorenetworksConfig holds configuration for the "corenetworks" DNS provider (Core-Networks)
// See https://www.core-networks.de/
type CorenetworksConfig struct {
	Login              string // CORENETWORKS_LOGIN: The username of the API account
	Password           string // CORENETWORKS_PASSWORD: The password
	PollingInterval    string // CORENETWORKS_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // CORENETWORKS_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	SequenceInterval   string // CORENETWORKS_SEQUENCE_INTERVAL: Time between sequential requests in seconds (Default: 60)
	TTL                string // CORENETWORKS_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 3600)
}

// newProvider builds the lego DNS challenge provider using strong types
// Credentials are passed directly to lego and never written to the process environment
func (c *CorenetworksConfig) newProvider() (challenge.Provider, error) {
	cfg := prov.NewDefaultConfig()
	if c.Login != "" {
		cfg.Login = c.Login
	}
	if c.Password != "" {
		cfg.Password = c.Password
	}
	if c.PollingInterval != "" {
		v, err := strconv.Atoi(c.PollingInterval)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"pollingInterval\": %w", err)
		}
		cfg.PollingInterval = time.Duration(v) * time.Second
	}
	if c.PropagationTimeout != "" {
		v, err := strconv.Atoi(c.PropagationTimeout)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"propagationTimeout\": %w", err)
		}
		cfg.PropagationTimeout = time.Duration(v) * time.Second
	}
	if c.SequenceInterval != "" {
		v, err := strconv.Atoi(c.SequenceInterval)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"sequenceInterval\": %w", err)
		}
		cfg.SequenceInterval = time.Duration(v) * time.Second
	}
	if c.TTL != "" {
		v, err := strconv.Atoi(c.TTL)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"ttl\": %w", err)
		}
		cfg.TTL = v
	}
	return prov.NewDNSProviderConfig(cfg)
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *CorenetworksConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"corenetworks\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "login", "CORENETWORKS_LOGIN":
			c.Login = val
		case "password", "CORENETWORKS_PASSWORD":
			c.Password = val
		case "pollingInterval", "CORENETWORKS_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "CORENETWORKS_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "sequenceInterval", "CORENETWORKS_SEQUENCE_INTERVAL":
			c.SequenceInterval = val
		case "ttl", "CORENETWORKS_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"corenetworks\"", key)
		}
	}
	return nil
}
