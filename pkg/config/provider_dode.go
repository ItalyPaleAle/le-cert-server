// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// DodeConfig holds configuration for the "dode" DNS provider (Domain Offensive (do.de))
// See https://www.do.de/
type DodeConfig struct {
	Token              string // DODE_TOKEN: API token
	HTTPTimeout        string // DODE_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // DODE_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // DODE_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	SequenceInterval   string // DODE_SEQUENCE_INTERVAL: Time between sequential requests in seconds (Default: 60)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *DodeConfig) envVars() map[string]string {
	m := make(map[string]string, 5)
	if c.Token != "" {
		m["DODE_TOKEN"] = c.Token
	}
	if c.HTTPTimeout != "" {
		m["DODE_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["DODE_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["DODE_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.SequenceInterval != "" {
		m["DODE_SEQUENCE_INTERVAL"] = c.SequenceInterval
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *DodeConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"dode\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "token", "DODE_TOKEN":
			c.Token = val
		case "httpTimeout", "DODE_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "DODE_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "DODE_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "sequenceInterval", "DODE_SEQUENCE_INTERVAL":
			c.SequenceInterval = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"dode\"", key)
		}
	}
	return nil
}
