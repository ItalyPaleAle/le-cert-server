// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// NodionConfig holds configuration for the "nodion" DNS provider (Nodion)
// See https://www.nodion.com
type NodionConfig struct {
	APIToken           string // NODION_API_TOKEN: The API token
	HTTPTimeout        string // NODION_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // NODION_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // NODION_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 120)
	TTL                string // NODION_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 120)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *NodionConfig) envVars() map[string]string {
	m := make(map[string]string, 5)
	if c.APIToken != "" {
		m["NODION_API_TOKEN"] = c.APIToken
	}
	if c.HTTPTimeout != "" {
		m["NODION_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["NODION_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["NODION_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["NODION_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *NodionConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"nodion\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "apiToken", "NODION_API_TOKEN":
			c.APIToken = val
		case "httpTimeout", "NODION_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "NODION_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "NODION_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "NODION_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"nodion\"", key)
		}
	}
	return nil
}
