// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"
	"strconv"
	"time"

	"github.com/go-acme/lego/v5/challenge"
	prov "github.com/go-acme/lego/v5/providers/dns/ngenix"
	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// NgenixConfig holds configuration for the "ngenix" DNS provider (Ngenix)
// See https://ngenix.net
type NgenixConfig struct {
	CustomerID         string // NGENIX_CUSTOMER_ID: Customer ID
	Token              string // NGENIX_TOKEN: API token
	Username           string // NGENIX_USERNAME: Username
	PollingInterval    string // NGENIX_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 20)
	PropagationTimeout string // NGENIX_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 600)
}

// newProvider builds the lego DNS challenge provider using strong types
// Credentials are passed directly to lego and never written to the process environment
func (c *NgenixConfig) newProvider() (challenge.Provider, error) {
	cfg := prov.NewDefaultConfig()
	if c.CustomerID != "" {
		cfg.CustomerID = c.CustomerID
	}
	if c.Token != "" {
		cfg.Token = c.Token
	}
	if c.Username != "" {
		cfg.Username = c.Username
	}
	if c.PollingInterval != "" {
		v, err := strconv.Atoi(c.PollingInterval)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"pollingInterval\": %w", err)
		}
		cfg.PollingInterval = time.Duration(v) * time.Second
	}
	if c.PropagationTimeout != "" {
		v, err := strconv.Atoi(c.PropagationTimeout)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"propagationTimeout\": %w", err)
		}
		cfg.PropagationTimeout = time.Duration(v) * time.Second
	}
	return prov.NewDNSProviderConfig(cfg)
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases
func (c *NgenixConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"ngenix\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "customerID", "NGENIX_CUSTOMER_ID":
			c.CustomerID = val
		case "token", "NGENIX_TOKEN":
			c.Token = val
		case "username", "NGENIX_USERNAME":
			c.Username = val
		case "pollingInterval", "NGENIX_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "NGENIX_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"ngenix\"", key)
		}
	}
	return nil
}
