// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// Ns1Config holds configuration for the "ns1" DNS provider (NS1)
// See https://ns1.com
type Ns1Config struct {
	APIKey             string // NS1_API_KEY: API key
	HTTPTimeout        string // NS1_HTTP_TIMEOUT: API request timeout in seconds (Default: 10)
	PollingInterval    string // NS1_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // NS1_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	TTL                string // NS1_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 120)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *Ns1Config) envVars() map[string]string {
	m := make(map[string]string, 5)
	if c.APIKey != "" {
		m["NS1_API_KEY"] = c.APIKey
	}
	if c.HTTPTimeout != "" {
		m["NS1_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["NS1_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["NS1_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["NS1_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *Ns1Config) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"ns1\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "apiKey", "NS1_API_KEY":
			c.APIKey = val
		case "httpTimeout", "NS1_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "NS1_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "NS1_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "NS1_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"ns1\"", key)
		}
	}
	return nil
}
