// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// RimuhostingConfig holds configuration for the "rimuhosting" DNS provider (RimuHosting)
// See https://rimuhosting.com
type RimuhostingConfig struct {
	APIKey             string // RIMUHOSTING_API_KEY: User API key
	HTTPTimeout        string // RIMUHOSTING_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // RIMUHOSTING_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // RIMUHOSTING_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	TTL                string // RIMUHOSTING_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 3600)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *RimuhostingConfig) envVars() map[string]string {
	m := make(map[string]string, 5)
	if c.APIKey != "" {
		m["RIMUHOSTING_API_KEY"] = c.APIKey
	}
	if c.HTTPTimeout != "" {
		m["RIMUHOSTING_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["RIMUHOSTING_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["RIMUHOSTING_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["RIMUHOSTING_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *RimuhostingConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"rimuhosting\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "apiKey", "RIMUHOSTING_API_KEY":
			c.APIKey = val
		case "httpTimeout", "RIMUHOSTING_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "RIMUHOSTING_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "RIMUHOSTING_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "RIMUHOSTING_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"rimuhosting\"", key)
		}
	}
	return nil
}
