// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"
	"strconv"
	"time"

	"github.com/go-acme/lego/v4/challenge"
	prov "github.com/go-acme/lego/v4/providers/dns/inwx"
	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// InwxConfig holds configuration for the "inwx" DNS provider (INWX)
// See https://www.inwx.de/en
type InwxConfig struct {
	Password           string // INWX_PASSWORD: Password
	Username           string // INWX_USERNAME: Username
	PollingInterval    string // INWX_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // INWX_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 360)
	Sandbox            string // INWX_SANDBOX: Activate the sandbox (boolean)
	SharedSecret       string // INWX_SHARED_SECRET: shared secret related to 2FA
	TTL                string // INWX_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 300)
}

// newProvider builds the lego DNS challenge provider using strong types
// Credentials are passed directly to lego and never written to the process environment
func (c *InwxConfig) newProvider() (challenge.Provider, error) {
	cfg := prov.NewDefaultConfig()
	if c.Password != "" {
		cfg.Password = c.Password
	}
	if c.Username != "" {
		cfg.Username = c.Username
	}
	if c.PollingInterval != "" {
		v, err := strconv.Atoi(c.PollingInterval)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"pollingInterval\": %w", err)
		}
		cfg.PollingInterval = time.Duration(v) * time.Second
	}
	if c.PropagationTimeout != "" {
		v, err := strconv.Atoi(c.PropagationTimeout)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"propagationTimeout\": %w", err)
		}
		cfg.PropagationTimeout = time.Duration(v) * time.Second
	}
	if c.Sandbox != "" {
		v, err := strconv.ParseBool(c.Sandbox)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"sandbox\": %w", err)
		}
		cfg.Sandbox = v
	}
	if c.SharedSecret != "" {
		cfg.SharedSecret = c.SharedSecret
	}
	if c.TTL != "" {
		v, err := strconv.Atoi(c.TTL)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"ttl\": %w", err)
		}
		cfg.TTL = v
	}
	return prov.NewDNSProviderConfig(cfg)
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *InwxConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"inwx\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "password", "INWX_PASSWORD":
			c.Password = val
		case "username", "INWX_USERNAME":
			c.Username = val
		case "pollingInterval", "INWX_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "INWX_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "sandbox", "INWX_SANDBOX":
			c.Sandbox = val
		case "sharedSecret", "INWX_SHARED_SECRET":
			c.SharedSecret = val
		case "ttl", "INWX_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"inwx\"", key)
		}
	}
	return nil
}
