// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// ZonomiConfig holds configuration for the "zonomi" DNS provider (Zonomi)
// See https://zonomi.com
type ZonomiConfig struct {
	APIKey             string // ZONOMI_API_KEY: User API key
	HTTPTimeout        string // ZONOMI_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // ZONOMI_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // ZONOMI_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	TTL                string // ZONOMI_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 3600)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *ZonomiConfig) envVars() map[string]string {
	m := make(map[string]string, 5)
	if c.APIKey != "" {
		m["ZONOMI_API_KEY"] = c.APIKey
	}
	if c.HTTPTimeout != "" {
		m["ZONOMI_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["ZONOMI_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["ZONOMI_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["ZONOMI_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *ZonomiConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"zonomi\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "apiKey", "ZONOMI_API_KEY":
			c.APIKey = val
		case "httpTimeout", "ZONOMI_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "ZONOMI_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "ZONOMI_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "ZONOMI_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"zonomi\"", key)
		}
	}
	return nil
}
