// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"
	"net/url"
	"strconv"
	"time"

	"github.com/go-acme/lego/v4/challenge"
	prov "github.com/go-acme/lego/v4/providers/dns/autodns"
	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// AutodnsConfig holds configuration for the "autodns" DNS provider (Autodns)
// See https://www.internetx.com/domains/autodns/
type AutodnsConfig struct {
	APIPassword        string // AUTODNS_API_PASSWORD: User Password
	APIUser            string // AUTODNS_API_USER: Username
	Context            string // AUTODNS_CONTEXT: API context (4 for production, 1 for testing. Defaults to 4)
	Endpoint           string // AUTODNS_ENDPOINT: API endpoint URL, defaults to https://api.autodns.com/v1/
	PollingInterval    string // AUTODNS_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // AUTODNS_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 120)
	TTL                string // AUTODNS_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 600)
}

// newProvider builds the lego DNS challenge provider using strong types
// Credentials are passed directly to lego and never written to the process environment
func (c *AutodnsConfig) newProvider() (challenge.Provider, error) {
	cfg := prov.NewDefaultConfig()
	if c.APIPassword != "" {
		cfg.Password = c.APIPassword
	}
	if c.APIUser != "" {
		cfg.Username = c.APIUser
	}
	if c.Context != "" {
		v, err := strconv.Atoi(c.Context)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"context\": %w", err)
		}
		cfg.Context = v
	}
	if c.Endpoint != "" {
		v, err := url.Parse(c.Endpoint)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"endpoint\": %w", err)
		}
		cfg.Endpoint = v
	}
	if c.PollingInterval != "" {
		v, err := strconv.Atoi(c.PollingInterval)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"pollingInterval\": %w", err)
		}
		cfg.PollingInterval = time.Duration(v) * time.Second
	}
	if c.PropagationTimeout != "" {
		v, err := strconv.Atoi(c.PropagationTimeout)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"propagationTimeout\": %w", err)
		}
		cfg.PropagationTimeout = time.Duration(v) * time.Second
	}
	if c.TTL != "" {
		v, err := strconv.Atoi(c.TTL)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"ttl\": %w", err)
		}
		cfg.TTL = v
	}
	return prov.NewDNSProviderConfig(cfg)
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *AutodnsConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"autodns\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "apiPassword", "AUTODNS_API_PASSWORD":
			c.APIPassword = val
		case "apiUser", "AUTODNS_API_USER":
			c.APIUser = val
		case "context", "AUTODNS_CONTEXT":
			c.Context = val
		case "endpoint", "AUTODNS_ENDPOINT":
			c.Endpoint = val
		case "pollingInterval", "AUTODNS_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "AUTODNS_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "AUTODNS_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"autodns\"", key)
		}
	}
	return nil
}
