// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// BranditConfig holds configuration for the "brandit" DNS provider (Brandit (deprecated))
// See https://www.brandit.com/
type BranditConfig struct {
	APIKey             string // BRANDIT_API_KEY: The API key
	APIUsername        string // BRANDIT_API_USERNAME: The API username
	HTTPTimeout        string // BRANDIT_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // BRANDIT_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // BRANDIT_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 600)
	TTL                string // BRANDIT_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 600)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *BranditConfig) envVars() map[string]string {
	m := make(map[string]string, 6)
	if c.APIKey != "" {
		m["BRANDIT_API_KEY"] = c.APIKey
	}
	if c.APIUsername != "" {
		m["BRANDIT_API_USERNAME"] = c.APIUsername
	}
	if c.HTTPTimeout != "" {
		m["BRANDIT_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["BRANDIT_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["BRANDIT_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["BRANDIT_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *BranditConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"brandit\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "apiKey", "BRANDIT_API_KEY":
			c.APIKey = val
		case "apiUsername", "BRANDIT_API_USERNAME":
			c.APIUsername = val
		case "httpTimeout", "BRANDIT_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "BRANDIT_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "BRANDIT_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "BRANDIT_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"brandit\"", key)
		}
	}
	return nil
}
