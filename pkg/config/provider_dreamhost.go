// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// DreamhostConfig holds configuration for the "dreamhost" DNS provider (DreamHost)
// See https://www.dreamhost.com
type DreamhostConfig struct {
	APIKey             string // DREAMHOST_API_KEY: The API key
	HTTPTimeout        string // DREAMHOST_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // DREAMHOST_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 60)
	PropagationTimeout string // DREAMHOST_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 3600)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *DreamhostConfig) envVars() map[string]string {
	m := make(map[string]string, 4)
	if c.APIKey != "" {
		m["DREAMHOST_API_KEY"] = c.APIKey
	}
	if c.HTTPTimeout != "" {
		m["DREAMHOST_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["DREAMHOST_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["DREAMHOST_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *DreamhostConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"dreamhost\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "apiKey", "DREAMHOST_API_KEY":
			c.APIKey = val
		case "httpTimeout", "DREAMHOST_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "DREAMHOST_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "DREAMHOST_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"dreamhost\"", key)
		}
	}
	return nil
}
