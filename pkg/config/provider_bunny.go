// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// BunnyConfig holds configuration for the "bunny" DNS provider (Bunny)
// See https://bunny.net
type BunnyConfig struct {
	APIKey             string // BUNNY_API_KEY: API key
	HTTPTimeout        string // BUNNY_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // BUNNY_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // BUNNY_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 120)
	TTL                string // BUNNY_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 60)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *BunnyConfig) envVars() map[string]string {
	m := make(map[string]string, 5)
	if c.APIKey != "" {
		m["BUNNY_API_KEY"] = c.APIKey
	}
	if c.HTTPTimeout != "" {
		m["BUNNY_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["BUNNY_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["BUNNY_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["BUNNY_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *BunnyConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"bunny\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "apiKey", "BUNNY_API_KEY":
			c.APIKey = val
		case "httpTimeout", "BUNNY_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "BUNNY_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "BUNNY_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "BUNNY_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"bunny\"", key)
		}
	}
	return nil
}
