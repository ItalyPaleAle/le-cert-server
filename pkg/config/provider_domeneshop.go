// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"
	"strconv"
	"time"

	"github.com/go-acme/lego/v4/challenge"
	prov "github.com/go-acme/lego/v4/providers/dns/domeneshop"
	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// DomeneshopConfig holds configuration for the "domeneshop" DNS provider (Domeneshop)
// See https://domene.shop
type DomeneshopConfig struct {
	APISecret          string // DOMENESHOP_API_SECRET: API secret
	APIToken           string // DOMENESHOP_API_TOKEN: API token
	PollingInterval    string // DOMENESHOP_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 20)
	PropagationTimeout string // DOMENESHOP_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 300)
}

// newProvider builds the lego DNS challenge provider using strong types
// Credentials are passed directly to lego and never written to the process environment
func (c *DomeneshopConfig) newProvider() (challenge.Provider, error) {
	cfg := prov.NewDefaultConfig()
	if c.APISecret != "" {
		cfg.APISecret = c.APISecret
	}
	if c.APIToken != "" {
		cfg.APIToken = c.APIToken
	}
	if c.PollingInterval != "" {
		v, err := strconv.Atoi(c.PollingInterval)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"pollingInterval\": %w", err)
		}
		cfg.PollingInterval = time.Duration(v) * time.Second
	}
	if c.PropagationTimeout != "" {
		v, err := strconv.Atoi(c.PropagationTimeout)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"propagationTimeout\": %w", err)
		}
		cfg.PropagationTimeout = time.Duration(v) * time.Second
	}
	return prov.NewDNSProviderConfig(cfg)
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *DomeneshopConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"domeneshop\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "apiSecret", "DOMENESHOP_API_SECRET":
			c.APISecret = val
		case "apiToken", "DOMENESHOP_API_TOKEN":
			c.APIToken = val
		case "pollingInterval", "DOMENESHOP_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "DOMENESHOP_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"domeneshop\"", key)
		}
	}
	return nil
}
