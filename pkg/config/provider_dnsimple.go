// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// DnsimpleConfig holds configuration for the "dnsimple" DNS provider (DNSimple)
// See https://dnsimple.com/
type DnsimpleConfig struct {
	OAUTHToken         string // DNSIMPLE_OAUTH_TOKEN: OAuth token
	BaseURL            string // DNSIMPLE_BASE_URL: API endpoint URL
	PollingInterval    string // DNSIMPLE_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // DNSIMPLE_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	TTL                string // DNSIMPLE_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 120)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *DnsimpleConfig) envVars() map[string]string {
	m := make(map[string]string, 5)
	if c.OAUTHToken != "" {
		m["DNSIMPLE_OAUTH_TOKEN"] = c.OAUTHToken
	}
	if c.BaseURL != "" {
		m["DNSIMPLE_BASE_URL"] = c.BaseURL
	}
	if c.PollingInterval != "" {
		m["DNSIMPLE_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["DNSIMPLE_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["DNSIMPLE_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *DnsimpleConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"dnsimple\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "oauthToken", "DNSIMPLE_OAUTH_TOKEN":
			c.OAUTHToken = val
		case "baseURL", "DNSIMPLE_BASE_URL":
			c.BaseURL = val
		case "pollingInterval", "DNSIMPLE_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "DNSIMPLE_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "DNSIMPLE_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"dnsimple\"", key)
		}
	}
	return nil
}
