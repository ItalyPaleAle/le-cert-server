// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// SpaceshipConfig holds configuration for the "spaceship" DNS provider (Spaceship)
// See https://www.spaceship.com/
type SpaceshipConfig struct {
	APIKey             string // SPACESHIP_API_KEY: API key
	APISecret          string // SPACESHIP_API_SECRET: API secret
	HTTPTimeout        string // SPACESHIP_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // SPACESHIP_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // SPACESHIP_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	TTL                string // SPACESHIP_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 120)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *SpaceshipConfig) envVars() map[string]string {
	m := make(map[string]string, 6)
	if c.APIKey != "" {
		m["SPACESHIP_API_KEY"] = c.APIKey
	}
	if c.APISecret != "" {
		m["SPACESHIP_API_SECRET"] = c.APISecret
	}
	if c.HTTPTimeout != "" {
		m["SPACESHIP_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["SPACESHIP_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["SPACESHIP_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["SPACESHIP_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *SpaceshipConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"spaceship\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "apiKey", "SPACESHIP_API_KEY":
			c.APIKey = val
		case "apiSecret", "SPACESHIP_API_SECRET":
			c.APISecret = val
		case "httpTimeout", "SPACESHIP_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "SPACESHIP_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "SPACESHIP_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "SPACESHIP_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"spaceship\"", key)
		}
	}
	return nil
}
