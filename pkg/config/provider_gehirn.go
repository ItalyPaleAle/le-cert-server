// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"
	"strconv"
	"time"

	"github.com/go-acme/lego/v5/challenge"
	prov "github.com/go-acme/lego/v5/providers/dns/gehirn"
	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// GehirnConfig holds configuration for the "gehirn" DNS provider (Gehirn)
// See https://www.gehirn.jp/
type GehirnConfig struct {
	TokenID            string // GEHIRN_TOKEN_ID: Token ID
	TokenSecret        string // GEHIRN_TOKEN_SECRET: Token secret
	PollingInterval    string // GEHIRN_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // GEHIRN_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	TTL                string // GEHIRN_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 120)
}

// newProvider builds the lego DNS challenge provider using strong types
// Credentials are passed directly to lego and never written to the process environment
func (c *GehirnConfig) newProvider() (challenge.Provider, error) {
	cfg := prov.NewDefaultConfig()
	if c.TokenID != "" {
		cfg.TokenID = c.TokenID
	}
	if c.TokenSecret != "" {
		cfg.TokenSecret = c.TokenSecret
	}
	if c.PollingInterval != "" {
		v, err := strconv.Atoi(c.PollingInterval)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"pollingInterval\": %w", err)
		}
		cfg.PollingInterval = time.Duration(v) * time.Second
	}
	if c.PropagationTimeout != "" {
		v, err := strconv.Atoi(c.PropagationTimeout)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"propagationTimeout\": %w", err)
		}
		cfg.PropagationTimeout = time.Duration(v) * time.Second
	}
	if c.TTL != "" {
		v, err := strconv.Atoi(c.TTL)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"ttl\": %w", err)
		}
		cfg.TTL = v
	}
	return prov.NewDNSProviderConfig(cfg)
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases
func (c *GehirnConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"gehirn\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "tokenID", "GEHIRN_TOKEN_ID":
			c.TokenID = val
		case "tokenSecret", "GEHIRN_TOKEN_SECRET":
			c.TokenSecret = val
		case "pollingInterval", "GEHIRN_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "GEHIRN_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "GEHIRN_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"gehirn\"", key)
		}
	}
	return nil
}
