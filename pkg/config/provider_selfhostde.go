// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// SelfhostdeConfig holds configuration for the "selfhostde" DNS provider (SelfHost.(de|eu))
// See https://www.selfhost.de
type SelfhostdeConfig struct {
	Password           string // SELFHOSTDE_PASSWORD: Password
	RecordsMapping     string // SELFHOSTDE_RECORDS_MAPPING: Record IDs mapping with domains (ex: example.com:123:456,example.org:789,foo.example.com:147)
	Username           string // SELFHOSTDE_USERNAME: Username
	HTTPTimeout        string // SELFHOSTDE_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // SELFHOSTDE_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 30)
	PropagationTimeout string // SELFHOSTDE_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 240)
	TTL                string // SELFHOSTDE_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 120)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *SelfhostdeConfig) envVars() map[string]string {
	m := make(map[string]string, 7)
	if c.Password != "" {
		m["SELFHOSTDE_PASSWORD"] = c.Password
	}
	if c.RecordsMapping != "" {
		m["SELFHOSTDE_RECORDS_MAPPING"] = c.RecordsMapping
	}
	if c.Username != "" {
		m["SELFHOSTDE_USERNAME"] = c.Username
	}
	if c.HTTPTimeout != "" {
		m["SELFHOSTDE_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["SELFHOSTDE_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["SELFHOSTDE_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["SELFHOSTDE_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *SelfhostdeConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"selfhostde\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "password", "SELFHOSTDE_PASSWORD":
			c.Password = val
		case "recordsMapping", "SELFHOSTDE_RECORDS_MAPPING":
			c.RecordsMapping = val
		case "username", "SELFHOSTDE_USERNAME":
			c.Username = val
		case "httpTimeout", "SELFHOSTDE_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "SELFHOSTDE_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "SELFHOSTDE_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "SELFHOSTDE_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"selfhostde\"", key)
		}
	}
	return nil
}
