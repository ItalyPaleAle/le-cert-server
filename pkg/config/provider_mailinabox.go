// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"
	"strconv"
	"time"

	"github.com/go-acme/lego/v4/challenge"
	prov "github.com/go-acme/lego/v4/providers/dns/mailinabox"
	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// MailinaboxConfig holds configuration for the "mailinabox" DNS provider (Mail-in-a-Box)
// See https://mailinabox.email
type MailinaboxConfig struct {
	BaseURL            string // MAILINABOX_BASE_URL: Base API URL (ex: https://box.example.com)
	Email              string // MAILINABOX_EMAIL: User email
	Password           string // MAILINABOX_PASSWORD: User password
	PollingInterval    string // MAILINABOX_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 4)
	PropagationTimeout string // MAILINABOX_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 120)
}

// newProvider builds the lego DNS challenge provider using strong types
// Credentials are passed directly to lego and never written to the process environment
func (c *MailinaboxConfig) newProvider() (challenge.Provider, error) {
	cfg := prov.NewDefaultConfig()
	if c.BaseURL != "" {
		cfg.BaseURL = c.BaseURL
	}
	if c.Email != "" {
		cfg.Email = c.Email
	}
	if c.Password != "" {
		cfg.Password = c.Password
	}
	if c.PollingInterval != "" {
		v, err := strconv.Atoi(c.PollingInterval)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"pollingInterval\": %w", err)
		}
		cfg.PollingInterval = time.Duration(v) * time.Second
	}
	if c.PropagationTimeout != "" {
		v, err := strconv.Atoi(c.PropagationTimeout)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"propagationTimeout\": %w", err)
		}
		cfg.PropagationTimeout = time.Duration(v) * time.Second
	}
	return prov.NewDNSProviderConfig(cfg)
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *MailinaboxConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"mailinabox\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "baseURL", "MAILINABOX_BASE_URL":
			c.BaseURL = val
		case "email", "MAILINABOX_EMAIL":
			c.Email = val
		case "password", "MAILINABOX_PASSWORD":
			c.Password = val
		case "pollingInterval", "MAILINABOX_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "MAILINABOX_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"mailinabox\"", key)
		}
	}
	return nil
}
