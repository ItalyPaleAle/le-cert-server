// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"
	"strconv"
	"time"

	"github.com/go-acme/lego/v4/challenge"
	prov "github.com/go-acme/lego/v4/providers/dns/cloudxns"
	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// CloudxnsConfig holds configuration for the "cloudxns" DNS provider (CloudXNS (Deprecated))
// See https://github.com/go-acme/lego/issues/2323
type CloudxnsConfig struct {
	APIKey             string // CLOUDXNS_API_KEY: The API key
	SecretKey          string // CLOUDXNS_SECRET_KEY: The API secret key
	PollingInterval    string // CLOUDXNS_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: )
	PropagationTimeout string // CLOUDXNS_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: )
	TTL                string // CLOUDXNS_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: )
}

// newProvider builds the lego DNS challenge provider using strong types
// Credentials are passed directly to lego and never written to the process environment
func (c *CloudxnsConfig) newProvider() (challenge.Provider, error) {
	cfg := prov.NewDefaultConfig()
	if c.APIKey != "" {
		cfg.APIKey = c.APIKey
	}
	if c.SecretKey != "" {
		cfg.SecretKey = c.SecretKey
	}
	if c.PollingInterval != "" {
		v, err := strconv.Atoi(c.PollingInterval)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"pollingInterval\": %w", err)
		}
		cfg.PollingInterval = time.Duration(v) * time.Second
	}
	if c.PropagationTimeout != "" {
		v, err := strconv.Atoi(c.PropagationTimeout)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"propagationTimeout\": %w", err)
		}
		cfg.PropagationTimeout = time.Duration(v) * time.Second
	}
	if c.TTL != "" {
		v, err := strconv.Atoi(c.TTL)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"ttl\": %w", err)
		}
		cfg.TTL = v
	}
	return prov.NewDNSProviderConfig(cfg)
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *CloudxnsConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"cloudxns\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "apiKey", "CLOUDXNS_API_KEY":
			c.APIKey = val
		case "secretKey", "CLOUDXNS_SECRET_KEY":
			c.SecretKey = val
		case "pollingInterval", "CLOUDXNS_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "CLOUDXNS_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "CLOUDXNS_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"cloudxns\"", key)
		}
	}
	return nil
}
