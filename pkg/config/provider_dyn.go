// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// DynConfig holds configuration for the "dyn" DNS provider (Dyn)
// See https://dyn.com/
type DynConfig struct {
	CustomerName       string // DYN_CUSTOMER_NAME: Customer name
	Password           string // DYN_PASSWORD: Password
	UserName           string // DYN_USER_NAME: User name
	HTTPTimeout        string // DYN_HTTP_TIMEOUT: API request timeout in seconds (Default: 10)
	PollingInterval    string // DYN_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // DYN_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	TTL                string // DYN_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 120)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *DynConfig) envVars() map[string]string {
	m := make(map[string]string, 7)
	if c.CustomerName != "" {
		m["DYN_CUSTOMER_NAME"] = c.CustomerName
	}
	if c.Password != "" {
		m["DYN_PASSWORD"] = c.Password
	}
	if c.UserName != "" {
		m["DYN_USER_NAME"] = c.UserName
	}
	if c.HTTPTimeout != "" {
		m["DYN_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["DYN_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["DYN_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["DYN_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *DynConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"dyn\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "customerName", "DYN_CUSTOMER_NAME":
			c.CustomerName = val
		case "password", "DYN_PASSWORD":
			c.Password = val
		case "userName", "DYN_USER_NAME":
			c.UserName = val
		case "httpTimeout", "DYN_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "DYN_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "DYN_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "DYN_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"dyn\"", key)
		}
	}
	return nil
}
