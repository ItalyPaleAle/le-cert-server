// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// FreemyipConfig holds configuration for the "freemyip" DNS provider (freemyip.com)
// See https://freemyip.com/
type FreemyipConfig struct {
	Token              string // FREEMYIP_TOKEN: Account token
	HTTPTimeout        string // FREEMYIP_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // FREEMYIP_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // FREEMYIP_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	SequenceInterval   string // FREEMYIP_SEQUENCE_INTERVAL: Time between sequential requests in seconds (Default: 60)
	TTL                string // FREEMYIP_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 3600)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *FreemyipConfig) envVars() map[string]string {
	m := make(map[string]string, 6)
	if c.Token != "" {
		m["FREEMYIP_TOKEN"] = c.Token
	}
	if c.HTTPTimeout != "" {
		m["FREEMYIP_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["FREEMYIP_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["FREEMYIP_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.SequenceInterval != "" {
		m["FREEMYIP_SEQUENCE_INTERVAL"] = c.SequenceInterval
	}
	if c.TTL != "" {
		m["FREEMYIP_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *FreemyipConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"freemyip\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "token", "FREEMYIP_TOKEN":
			c.Token = val
		case "httpTimeout", "FREEMYIP_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "FREEMYIP_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "FREEMYIP_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "sequenceInterval", "FREEMYIP_SEQUENCE_INTERVAL":
			c.SequenceInterval = val
		case "ttl", "FREEMYIP_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"freemyip\"", key)
		}
	}
	return nil
}
