// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// SafednsConfig holds configuration for the "safedns" DNS provider (ANS SafeDNS)
// See https://www.ans.co.uk/
type SafednsConfig struct {
	AuthToken          string // SAFEDNS_AUTH_TOKEN: Authentication token
	HTTPTimeout        string // SAFEDNS_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // SAFEDNS_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // SAFEDNS_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	TTL                string // SAFEDNS_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 120)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *SafednsConfig) envVars() map[string]string {
	m := make(map[string]string, 5)
	if c.AuthToken != "" {
		m["SAFEDNS_AUTH_TOKEN"] = c.AuthToken
	}
	if c.HTTPTimeout != "" {
		m["SAFEDNS_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["SAFEDNS_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["SAFEDNS_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["SAFEDNS_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *SafednsConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"safedns\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "authToken", "SAFEDNS_AUTH_TOKEN":
			c.AuthToken = val
		case "httpTimeout", "SAFEDNS_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "SAFEDNS_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "SAFEDNS_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "SAFEDNS_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"safedns\"", key)
		}
	}
	return nil
}
