// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"
	"strconv"
	"time"

	"github.com/go-acme/lego/v4/challenge"
	prov "github.com/go-acme/lego/v4/providers/dns/yandexcloud"
	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// YandexcloudConfig holds configuration for the "yandexcloud" DNS provider (Yandex Cloud)
// See https://cloud.yandex.com
type YandexcloudConfig struct {
	CloudFolderID           string // YANDEX_CLOUD_FOLDER_ID: The string id of folder (aka project) in Yandex Cloud
	CloudIAMToken           string // YANDEX_CLOUD_IAM_TOKEN: The base64 encoded json which contains information about iam token of service account with `dns.admin` permissions
	CloudPollingInterval    string // YANDEX_CLOUD_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	CloudPropagationTimeout string // YANDEX_CLOUD_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	CloudTTL                string // YANDEX_CLOUD_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 60)
}

// newProvider builds the lego DNS challenge provider using strong types
// Credentials are passed directly to lego and never written to the process environment
func (c *YandexcloudConfig) newProvider() (challenge.Provider, error) {
	cfg := prov.NewDefaultConfig()
	if c.CloudFolderID != "" {
		cfg.FolderID = c.CloudFolderID
	}
	if c.CloudIAMToken != "" {
		cfg.IamToken = c.CloudIAMToken
	}
	if c.CloudPollingInterval != "" {
		v, err := strconv.Atoi(c.CloudPollingInterval)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"cloudPollingInterval\": %w", err)
		}
		cfg.PollingInterval = time.Duration(v) * time.Second
	}
	if c.CloudPropagationTimeout != "" {
		v, err := strconv.Atoi(c.CloudPropagationTimeout)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"cloudPropagationTimeout\": %w", err)
		}
		cfg.PropagationTimeout = time.Duration(v) * time.Second
	}
	if c.CloudTTL != "" {
		v, err := strconv.Atoi(c.CloudTTL)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"cloudTTL\": %w", err)
		}
		cfg.TTL = v
	}
	return prov.NewDNSProviderConfig(cfg)
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *YandexcloudConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"yandexcloud\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "cloudFolderID", "YANDEX_CLOUD_FOLDER_ID":
			c.CloudFolderID = val
		case "cloudIAMToken", "YANDEX_CLOUD_IAM_TOKEN":
			c.CloudIAMToken = val
		case "cloudPollingInterval", "YANDEX_CLOUD_POLLING_INTERVAL":
			c.CloudPollingInterval = val
		case "cloudPropagationTimeout", "YANDEX_CLOUD_PROPAGATION_TIMEOUT":
			c.CloudPropagationTimeout = val
		case "cloudTTL", "YANDEX_CLOUD_TTL":
			c.CloudTTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"yandexcloud\"", key)
		}
	}
	return nil
}
