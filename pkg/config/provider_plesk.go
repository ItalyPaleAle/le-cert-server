// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// PleskConfig holds configuration for the "plesk" DNS provider (plesk.com)
// See https://www.plesk.com/
type PleskConfig struct {
	Password           string // PLESK_PASSWORD: API password
	ServerBaseURL      string // PLESK_SERVER_BASE_URL: Base URL of the server (ex: https://plesk.myserver.com:8443)
	Username           string // PLESK_USERNAME: API username
	HTTPTimeout        string // PLESK_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // PLESK_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // PLESK_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	TTL                string // PLESK_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 300)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *PleskConfig) envVars() map[string]string {
	m := make(map[string]string, 7)
	if c.Password != "" {
		m["PLESK_PASSWORD"] = c.Password
	}
	if c.ServerBaseURL != "" {
		m["PLESK_SERVER_BASE_URL"] = c.ServerBaseURL
	}
	if c.Username != "" {
		m["PLESK_USERNAME"] = c.Username
	}
	if c.HTTPTimeout != "" {
		m["PLESK_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["PLESK_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["PLESK_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["PLESK_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *PleskConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"plesk\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "password", "PLESK_PASSWORD":
			c.Password = val
		case "serverBaseURL", "PLESK_SERVER_BASE_URL":
			c.ServerBaseURL = val
		case "username", "PLESK_USERNAME":
			c.Username = val
		case "httpTimeout", "PLESK_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "PLESK_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "PLESK_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "PLESK_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"plesk\"", key)
		}
	}
	return nil
}
