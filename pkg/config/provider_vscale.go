// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// VscaleConfig holds configuration for the "vscale" DNS provider (Vscale)
// See https://vscale.io/
type VscaleConfig struct {
	APIToken           string // VSCALE_API_TOKEN: API token
	BaseURL            string // VSCALE_BASE_URL: API endpoint URL
	HTTPTimeout        string // VSCALE_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // VSCALE_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // VSCALE_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 120)
	TTL                string // VSCALE_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 60)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *VscaleConfig) envVars() map[string]string {
	m := make(map[string]string, 6)
	if c.APIToken != "" {
		m["VSCALE_API_TOKEN"] = c.APIToken
	}
	if c.BaseURL != "" {
		m["VSCALE_BASE_URL"] = c.BaseURL
	}
	if c.HTTPTimeout != "" {
		m["VSCALE_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["VSCALE_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["VSCALE_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["VSCALE_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *VscaleConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"vscale\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "apiToken", "VSCALE_API_TOKEN":
			c.APIToken = val
		case "baseURL", "VSCALE_BASE_URL":
			c.BaseURL = val
		case "httpTimeout", "VSCALE_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "VSCALE_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "VSCALE_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "VSCALE_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"vscale\"", key)
		}
	}
	return nil
}
