// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"
	"strconv"
	"time"

	"github.com/go-acme/lego/v4/challenge"
	prov "github.com/go-acme/lego/v4/providers/dns/selectelv2"
	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// Selectelv2Config holds configuration for the "selectelv2" DNS provider (Selectel v2)
// See https://selectel.ru
type Selectelv2Config struct {
	AccountID          string // SELECTELV2_ACCOUNT_ID: Selectel account ID (INT)
	Password           string // SELECTELV2_PASSWORD: Openstack username's password
	ProjectID          string // SELECTELV2_PROJECT_ID: Cloud project ID (UUID)
	Username           string // SELECTELV2_USERNAME: Openstack username
	AuthRegion         string // SELECTELV2_AUTH_REGION: Location for auth endpoint like ResellAPI or Keystone (default: 'ru-1')
	AuthURL            string // SELECTELV2_AUTH_URL: Identity endpoint (defaul: 'https://cloud.api.selcloud.ru/identity/v3/')
	BaseURL            string // SELECTELV2_BASE_URL: API endpoint URL
	PollingInterval    string // SELECTELV2_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 5)
	PropagationTimeout string // SELECTELV2_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 120)
	TTL                string // SELECTELV2_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 60)
	UserDomainName     string // SELECTELV2_USER_DOMAIN_NAME: To specify the domain name (account ID) where the user is located. (default: SELECTELV2_ACCOUNT_ID)
}

// newProvider builds the lego DNS challenge provider using strong types
// Credentials are passed directly to lego and never written to the process environment
func (c *Selectelv2Config) newProvider() (challenge.Provider, error) {
	cfg := prov.NewDefaultConfig()
	if c.AccountID != "" {
		cfg.DomainName = c.AccountID
	}
	if c.Password != "" {
		cfg.Password = c.Password
	}
	if c.ProjectID != "" {
		cfg.ProjectID = c.ProjectID
	}
	if c.Username != "" {
		cfg.Username = c.Username
	}
	if c.AuthRegion != "" {
		cfg.AuthRegion = c.AuthRegion
	}
	if c.AuthURL != "" {
		cfg.AuthURL = c.AuthURL
	}
	if c.BaseURL != "" {
		cfg.BaseURL = c.BaseURL
	}
	if c.PollingInterval != "" {
		v, err := strconv.Atoi(c.PollingInterval)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"pollingInterval\": %w", err)
		}
		cfg.PollingInterval = time.Duration(v) * time.Second
	}
	if c.PropagationTimeout != "" {
		v, err := strconv.Atoi(c.PropagationTimeout)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"propagationTimeout\": %w", err)
		}
		cfg.PropagationTimeout = time.Duration(v) * time.Second
	}
	if c.TTL != "" {
		v, err := strconv.Atoi(c.TTL)
		if err != nil {
			return nil, fmt.Errorf("invalid value for \"ttl\": %w", err)
		}
		cfg.TTL = v
	}
	if c.UserDomainName != "" {
		cfg.UserDomainName = c.UserDomainName
	}
	return prov.NewDNSProviderConfig(cfg)
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *Selectelv2Config) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"selectelv2\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "accountID", "SELECTELV2_ACCOUNT_ID":
			c.AccountID = val
		case "password", "SELECTELV2_PASSWORD":
			c.Password = val
		case "projectID", "SELECTELV2_PROJECT_ID":
			c.ProjectID = val
		case "username", "SELECTELV2_USERNAME":
			c.Username = val
		case "authRegion", "SELECTELV2_AUTH_REGION":
			c.AuthRegion = val
		case "authURL", "SELECTELV2_AUTH_URL":
			c.AuthURL = val
		case "baseURL", "SELECTELV2_BASE_URL":
			c.BaseURL = val
		case "pollingInterval", "SELECTELV2_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "SELECTELV2_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "SELECTELV2_TTL":
			c.TTL = val
		case "userDomainName", "SELECTELV2_USER_DOMAIN_NAME":
			c.UserDomainName = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"selectelv2\"", key)
		}
	}
	return nil
}
