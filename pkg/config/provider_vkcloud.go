// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// VkcloudConfig holds configuration for the "vkcloud" DNS provider (VK Cloud)
// See https://mcs.mail.ru/
type VkcloudConfig struct {
	CloudPassword           string // VK_CLOUD_PASSWORD: Password for VK Cloud account
	CloudProjectID          string // VK_CLOUD_PROJECT_ID: String ID of project in VK Cloud
	CloudUsername           string // VK_CLOUD_USERNAME: Email of VK Cloud account
	CloudDNSEndpoint        string // VK_CLOUD_DNS_ENDPOINT: URL of DNS API. Defaults to https://mcs.mail.ru/public-dns but can be changed for usage with private clouds
	CloudDomainName         string // VK_CLOUD_DOMAIN_NAME: Openstack users domain name. Defaults to `users` but can be changed for usage with private clouds
	CloudIdentityEndpoint   string // VK_CLOUD_IDENTITY_ENDPOINT: URL of OpenStack Auth API, Defaults to https://infra.mail.ru:35357/v3/ but can be changed for usage with private clouds
	CloudPollingInterval    string // VK_CLOUD_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	CloudPropagationTimeout string // VK_CLOUD_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	CloudTTL                string // VK_CLOUD_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 60)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *VkcloudConfig) envVars() map[string]string {
	m := make(map[string]string, 9)
	if c.CloudPassword != "" {
		m["VK_CLOUD_PASSWORD"] = c.CloudPassword
	}
	if c.CloudProjectID != "" {
		m["VK_CLOUD_PROJECT_ID"] = c.CloudProjectID
	}
	if c.CloudUsername != "" {
		m["VK_CLOUD_USERNAME"] = c.CloudUsername
	}
	if c.CloudDNSEndpoint != "" {
		m["VK_CLOUD_DNS_ENDPOINT"] = c.CloudDNSEndpoint
	}
	if c.CloudDomainName != "" {
		m["VK_CLOUD_DOMAIN_NAME"] = c.CloudDomainName
	}
	if c.CloudIdentityEndpoint != "" {
		m["VK_CLOUD_IDENTITY_ENDPOINT"] = c.CloudIdentityEndpoint
	}
	if c.CloudPollingInterval != "" {
		m["VK_CLOUD_POLLING_INTERVAL"] = c.CloudPollingInterval
	}
	if c.CloudPropagationTimeout != "" {
		m["VK_CLOUD_PROPAGATION_TIMEOUT"] = c.CloudPropagationTimeout
	}
	if c.CloudTTL != "" {
		m["VK_CLOUD_TTL"] = c.CloudTTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *VkcloudConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"vkcloud\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "cloudPassword", "VK_CLOUD_PASSWORD":
			c.CloudPassword = val
		case "cloudProjectID", "VK_CLOUD_PROJECT_ID":
			c.CloudProjectID = val
		case "cloudUsername", "VK_CLOUD_USERNAME":
			c.CloudUsername = val
		case "cloudDNSEndpoint", "VK_CLOUD_DNS_ENDPOINT":
			c.CloudDNSEndpoint = val
		case "cloudDomainName", "VK_CLOUD_DOMAIN_NAME":
			c.CloudDomainName = val
		case "cloudIdentityEndpoint", "VK_CLOUD_IDENTITY_ENDPOINT":
			c.CloudIdentityEndpoint = val
		case "cloudPollingInterval", "VK_CLOUD_POLLING_INTERVAL":
			c.CloudPollingInterval = val
		case "cloudPropagationTimeout", "VK_CLOUD_PROPAGATION_TIMEOUT":
			c.CloudPropagationTimeout = val
		case "cloudTTL", "VK_CLOUD_TTL":
			c.CloudTTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"vkcloud\"", key)
		}
	}
	return nil
}
