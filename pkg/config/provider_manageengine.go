// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// ManageengineConfig holds configuration for the "manageengine" DNS provider (ManageEngine CloudDNS)
// See https://clouddns.manageengine.com
type ManageengineConfig struct {
	ClientID           string // MANAGEENGINE_CLIENT_ID: Client ID
	ClientSecret       string // MANAGEENGINE_CLIENT_SECRET: Client Secret
	PollingInterval    string // MANAGEENGINE_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PropagationTimeout string // MANAGEENGINE_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	TTL                string // MANAGEENGINE_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 120)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *ManageengineConfig) envVars() map[string]string {
	m := make(map[string]string, 5)
	if c.ClientID != "" {
		m["MANAGEENGINE_CLIENT_ID"] = c.ClientID
	}
	if c.ClientSecret != "" {
		m["MANAGEENGINE_CLIENT_SECRET"] = c.ClientSecret
	}
	if c.PollingInterval != "" {
		m["MANAGEENGINE_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["MANAGEENGINE_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["MANAGEENGINE_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *ManageengineConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"manageengine\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "clientID", "MANAGEENGINE_CLIENT_ID":
			c.ClientID = val
		case "clientSecret", "MANAGEENGINE_CLIENT_SECRET":
			c.ClientSecret = val
		case "pollingInterval", "MANAGEENGINE_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "MANAGEENGINE_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "MANAGEENGINE_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"manageengine\"", key)
		}
	}
	return nil
}
