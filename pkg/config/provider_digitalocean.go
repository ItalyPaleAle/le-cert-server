// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// DigitaloceanConfig holds configuration for the "digitalocean" DNS provider (Digital Ocean)
// See https://www.digitalocean.com/docs/networking/dns/
type DigitaloceanConfig struct {
	AuthToken          string // DO_AUTH_TOKEN: Authentication token
	APIURL             string // DO_API_URL: The URL of the API
	HTTPTimeout        string // DO_HTTP_TIMEOUT: API request timeout in seconds (Default: 30)
	PollingInterval    string // DO_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 5)
	PropagationTimeout string // DO_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	TTL                string // DO_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 30)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *DigitaloceanConfig) envVars() map[string]string {
	m := make(map[string]string, 6)
	if c.AuthToken != "" {
		m["DO_AUTH_TOKEN"] = c.AuthToken
	}
	if c.APIURL != "" {
		m["DO_API_URL"] = c.APIURL
	}
	if c.HTTPTimeout != "" {
		m["DO_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.PollingInterval != "" {
		m["DO_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PropagationTimeout != "" {
		m["DO_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.TTL != "" {
		m["DO_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *DigitaloceanConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"digitalocean\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "authToken", "DO_AUTH_TOKEN":
			c.AuthToken = val
		case "apiURL", "DO_API_URL":
			c.APIURL = val
		case "httpTimeout", "DO_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "pollingInterval", "DO_POLLING_INTERVAL":
			c.PollingInterval = val
		case "propagationTimeout", "DO_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "ttl", "DO_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"digitalocean\"", key)
		}
	}
	return nil
}
