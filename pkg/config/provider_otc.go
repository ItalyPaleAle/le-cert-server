// Code generated by gen-dns-providers; DO NOT EDIT.

package config

import (
	"fmt"

	yaml "sigs.k8s.io/yaml/goyaml.v3"
)

// OtcConfig holds configuration for the "otc" DNS provider (Open Telekom Cloud)
// See https://cloud.telekom.de/en
type OtcConfig struct {
	DomainName         string // OTC_DOMAIN_NAME: Domain name
	Password           string // OTC_PASSWORD: Password
	ProjectName        string // OTC_PROJECT_NAME: Project name
	UserName           string // OTC_USER_NAME: User name
	HTTPTimeout        string // OTC_HTTP_TIMEOUT: API request timeout in seconds (Default: 10)
	IdentityEndpoint   string // OTC_IDENTITY_ENDPOINT: Identity endpoint URL (default: https://iam.eu-de.otc.t-systems.com:443/v3/auth/tokens)
	PollingInterval    string // OTC_POLLING_INTERVAL: Time between DNS propagation check in seconds (Default: 2)
	PrivateZone        string // OTC_PRIVATE_ZONE: Set to true to use private zones only (default: use public zones only)
	PropagationTimeout string // OTC_PROPAGATION_TIMEOUT: Maximum waiting time for DNS propagation in seconds (Default: 60)
	SequenceInterval   string // OTC_SEQUENCE_INTERVAL: Time between sequential requests in seconds (Default: 60)
	TTL                string // OTC_TTL: The TTL of the TXT record used for the DNS challenge in seconds (Default: 300)
}

// envVars returns the lego environment variables for the populated (non-empty) fields
func (c *OtcConfig) envVars() map[string]string {
	m := make(map[string]string, 11)
	if c.DomainName != "" {
		m["OTC_DOMAIN_NAME"] = c.DomainName
	}
	if c.Password != "" {
		m["OTC_PASSWORD"] = c.Password
	}
	if c.ProjectName != "" {
		m["OTC_PROJECT_NAME"] = c.ProjectName
	}
	if c.UserName != "" {
		m["OTC_USER_NAME"] = c.UserName
	}
	if c.HTTPTimeout != "" {
		m["OTC_HTTP_TIMEOUT"] = c.HTTPTimeout
	}
	if c.IdentityEndpoint != "" {
		m["OTC_IDENTITY_ENDPOINT"] = c.IdentityEndpoint
	}
	if c.PollingInterval != "" {
		m["OTC_POLLING_INTERVAL"] = c.PollingInterval
	}
	if c.PrivateZone != "" {
		m["OTC_PRIVATE_ZONE"] = c.PrivateZone
	}
	if c.PropagationTimeout != "" {
		m["OTC_PROPAGATION_TIMEOUT"] = c.PropagationTimeout
	}
	if c.SequenceInterval != "" {
		m["OTC_SEQUENCE_INTERVAL"] = c.SequenceInterval
	}
	if c.TTL != "" {
		m["OTC_TTL"] = c.TTL
	}
	return m
}

// UnmarshalYAML decodes the provider credentials
// It accepts the normalized name, the raw lego environment variable, and documented aliases; unknown keys error
func (c *OtcConfig) UnmarshalYAML(value *yaml.Node) error {
	if value.Kind != yaml.MappingNode {
		return fmt.Errorf("dnsCredentials for DNS provider \"otc\" must be a map")
	}
	for i := 0; i+1 < len(value.Content); i += 2 {
		var key string
		err := value.Content[i].Decode(&key)
		if err != nil {
			return err
		}
		var val string
		err = value.Content[i+1].Decode(&val)
		if err != nil {
			return err
		}
		switch key {
		case "domainName", "OTC_DOMAIN_NAME":
			c.DomainName = val
		case "password", "OTC_PASSWORD":
			c.Password = val
		case "projectName", "OTC_PROJECT_NAME":
			c.ProjectName = val
		case "userName", "OTC_USER_NAME":
			c.UserName = val
		case "httpTimeout", "OTC_HTTP_TIMEOUT":
			c.HTTPTimeout = val
		case "identityEndpoint", "OTC_IDENTITY_ENDPOINT":
			c.IdentityEndpoint = val
		case "pollingInterval", "OTC_POLLING_INTERVAL":
			c.PollingInterval = val
		case "privateZone", "OTC_PRIVATE_ZONE":
			c.PrivateZone = val
		case "propagationTimeout", "OTC_PROPAGATION_TIMEOUT":
			c.PropagationTimeout = val
		case "sequenceInterval", "OTC_SEQUENCE_INTERVAL":
			c.SequenceInterval = val
		case "ttl", "OTC_TTL":
			c.TTL = val
		default:
			return fmt.Errorf("unknown credential key %q for DNS provider \"otc\"", key)
		}
	}
	return nil
}
